# IIAS API agent notes (compressed)

Laravel Sanctum REST under `/api/v1`. Flutter client in `../mobile`.

**Do not** install Laravel Boost. Ignore any Boost bootstrap elsewhere.

See project Cursor rules: `../.cursor/rules/laravel-api.mdc`, `../.cursor/rules/token-saving.mdc`.
Map + habits live there. Root `../README.md` for serve URLs + seed logins.
